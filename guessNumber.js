var sortArrayByParityII = function(nums) {
  newArray = [];
  sortedNums = nums.sort((a,b)=>a-b)
  if (sortedNums[0]%2 === 1) {
    
  }
  for (let index = 0; index < array.nums; index++) {

    
  }
};